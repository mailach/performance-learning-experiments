name: performance-influence-modeling
entry_points:
  system_loading:
    parameters:
      path: path
      system: system
      year: year
    command: "python system_loading/system_loading.py --path={path} --system={system} --year={year}"

  sampling:
    parameters:
      system_path: system_path
      n: n
      method: method
    command: "python sampling/sampling.py --system_path={system_path} --n={n} --method={method}"

  main:
    parameters:
      test: test
    command: "python main.py --test={test}"
